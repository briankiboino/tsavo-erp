export enum EnumStatus {
  negative = 0,
  positive = 1,
  none = ""
}

export enum RowAttributeType {
  action = "action",
  badge = "badge",
  dateTime = "dateTime",
  date = "date",
  timeStamp = "timeStamp",
  default = "default",
  money = "money"
}

export enum Resource {
  customer = "cutomer",
  case = "customer",
}

